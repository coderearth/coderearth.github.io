<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Tutorial | Coder Earth]]></title>
  <link href="http://coderearth.org/blog/categories/tutorial/atom.xml" rel="self"/>
  <link href="http://coderearth.org/"/>
  <updated>2014-11-22T22:00:32+05:30</updated>
  <id>http://coderearth.org/</id>
  <author>
    <name><![CDATA[Coder]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Installing and Running Hadoop 2.5 on Windows 7]]></title>
    <link href="http://coderearth.org/blog/2014/11/21/installing-and-running-hadoop-2-dot-5-on-windows-7/"/>
    <updated>2014-11-21T14:47:40+05:30</updated>
    <id>http://coderearth.org/blog/2014/11/21/installing-and-running-hadoop-2-dot-5-on-windows-7</id>
    <content type="html"><![CDATA[<p>We’re going to see how to install and run a single node <a href="http://hadoop.apache.org/">Hadoop</a> cluster, version <a href="http://hadoop.apache.org/#19+November%2C+2014%3A+release+2.5.2+available">2.5</a> on a 64-bit Windows 7. For ease of reading, I have divided this tutorial into multiple chapters.</p>

<h2 id="prerequisites">Prerequisites</h2>
<ul>
  <li>Do not use Cygwin.</li>
  <li>You need to have admin privileges to your computer.</li>
  <li>Follow the <i>exact </i> versions of various softwares with the <i>exact</i> installation paths that are mentioned below.</li>
  <li>You need a <i>good</i> internet connection, as quite a bit of downloading is involved.</li>
</ul>

<h2 id="chapter-1-install-java--configure-path">Chapter 1: Install JAVA &amp; Configure PATH</h2>

<ol>
  <li><a href="http://www.oracle.com/technetwork/java/javase/downloads/jdk7-downloads-1880260.html">Download JDK 1.7</a> and install it in <code>C:\Java\jdk1.7.0_71</code>.</li>
  <li>Set the <code>JAVA_HOME</code> path variable.
    <ol>
      <li>Right-click the My Computer icon on your desktop and select Properties.</li>
      <li>Click the Advanced system settings on the left pane.</li>
      <li>Click the Environment Variables button.</li>
      <li>Under System Variables, click New.</li>
      <li>Enter the variable name as JAVA_HOME.</li>
      <li>Enter the variable value as <code>C:\Java\jdk1.7.0_71</code>.</li>
      <li>Click OK</li>
    </ol>
  </li>
  <li>Add <code>%JAVA_HOME%\bin</code> to <code>Path</code>
    <ol>
      <li>Under System Variables, find the Path variable.</li>
      <li>Click Edit</li>
      <li>Prepend <code>%JAVA_HOME%\bin;</code></li>
      <li>Click OK</li>
    </ol>
  </li>
  <li>
    <p>Verify the JAVA setup.</p>

    <p><img src="http://i.imgur.com/wEexnBj.png"></p>
  </li>
</ol>

<p>Congratulations, you can now proceed to Chapter 2: Preparing Environment.</p>

<h2 id="chapter-2-preparing-environment">Chapter 2: Preparing Environment</h2>

<h4 id="part-1-download--extract-hadoop-source">Part 1: Download &amp; Extract Hadoop Source</h4>

<ol>
  <li>I use <a href="https://code.google.com/p/conemu-maximus5/">ConEmu</a> as my console, but the windows command prompt should suffice, as well.</li>
  <li>We will be needing <a href="http://www.7-zip.org/download.html">7zip</a> for extracting some archives later. Download &amp; Install it if you don’t have already.</li>
  <li>Download the Hadoop 2.5.2 stable source from <a href="http://mirrors.ibiblio.org/apache/hadoop/common/stable2/hadoop-2.5.2-src.tar.gz">here</a>.</li>
  <li>Extract the <code>.tar.gz</code> file with 7zip twice to get the <code>hadoop-2.5.2-src</code> folder.</li>
  <li>Let’s copy the contents of the above folder to <code>C:\hadoop</code>.</li>
</ol>

<p>At this stage, we have the hadoop source. We now have to build hadoop - to do this, we have to install some dependencies.</p>

<h4 id="part-2-installing-windows-dependencies">Part 2: Installing Windows Dependencies</h4>

<h5 id="dependency-1-apache-maven">Dependency 1: Apache Maven</h5>

<ol>
  <li><a href="http://maven.apache.org/download.cgi">Download Apache Maven 3.2.3</a></li>
  <li>Just like we did with JAVA, Set the <code>M2_HOME</code> path variable as <code>C:\apache-maven-3.2.3</code> and prepend <code>%M2_HOME%\bin;</code> to the system <code>Path</code> variable.</li>
  <li>
    <p>Verify maven installation by typing <code>mvn --version</code> in a command prompt.</p>

    <p><img src="http://i.imgur.com/gINQAu8.png"></p>
  </li>
</ol>

<h5 id="dependency-2--protocol-buffers-250">Dependency 2:  Protocol Buffers 2.5.0</h5>

<p>Protocol Buffers is Google’s efficient data interchange format. Read more <a href="https://developers.google.com/protocol-buffers/">here</a>.</p>

<ol>
  <li>Download Protocol Buffers 2.5.0</li>
  <li>Extract it to <code>C:\protoc-2.5.0-win32</code> and prepend it to the system <code>Path</code> variable.</li>
  <li>Verify that <code>protoc.exe</code> is in the <code>Path</code>.</li>
</ol>

<div><table class="CodeRay"><tr>
  <td class="line-numbers"><pre><a href="#n1" name="n1">1</a>
<a href="#n2" name="n2">2</a>
</pre></td>
  <td class="code"><pre>C:\Users\suhas&gt;where protoc
C:\protoc-2.5.0-win32\protoc.exe
</pre></td>
</tr></table>
</div>

<h5 id="dependency-3-windows-sdk">Dependency 3: Windows SDK</h5>

<p>Download and install the Windows SDK from <a href="http://www.microsoft.com/en-us/download/details.aspx?id=8279">here</a>. Once it is downloaded and installed, it will open a Help Manager window, where in select only Windows SDK to be updated.</p>

<p><img src="http://i.picasion.com/pic78/04ecafd50f5df11683b9a4eceb11cae9.gif"></p>

<h5 id="dependency-4-unix-command-line-tools">Dependency 4: Unix command-line tools</h5>

<p>Specifically, we need Unix command-line tools from GnuWin32 or Cygwin: <code>sh</code>, <code>mkdir</code>, <code>rm</code>, <code>cp</code>, <code>tar</code>, <code>gzip</code>. Since however we won’t be using Cygwin, we’ll use GnuWin32.</p>

<ol>
  <li>Download GetGnuWin32 from <a href="http://sourceforge.net/projects/getgnuwin32/files/">here</a>.</li>
  <li><b>Please skip to step 5</b>. Steps 3 &amp; 4 are further information for the curious &amp; the adventurous. </li>
  <li>Note that GetGnuWin32 is <i>not</i> GnuWin32 itself, but a package maintenance utility. More on that <a href="http://getgnuwin32.sourceforge.net/">here</a>, if you’re interested to read that is.</li>
  <li>Since downloading from the GetGnuWin32 might take a while, and if you’re feeling miserly about your bandwidth usage, <i>and</i> if you know what you’re doing, you can skip this section altogether provided you have done either of the below:
    <ol>
      <li>If you have Cygwin, add its bin to <code>Path</code>, after making sure it has the above mentioned commands.</li>
      <li>Individually download the above commands from GnuWin32 and add them to your <code>Path</code>.</li>
    </ol>
  </li>
  <li>Click install on the GetGnuWin32 window, by default it should go to your <code>Downloads</code> folder.</li>
  <li>From a command prompt, go where <code>GetGnuWin32</code> was installed to, in my case, <code>C:\Users\suhas\Downloads\GetGnuWin32</code> and run <code>download.bat</code>. This step will take a while.</li>
  <li>Run <code>install C:\gnuwin32</code>.</li>
  <li>Append <code>C:\gnuwin32\bin</code> to your <code>Path</code>.</li>
</ol>

<h5 id="dependency-5-zlib-128">Dependency 5: Zlib 1.2.8</h5>

<p>To be fair, zlib comes as part of the above GnuWin32 package, but let’s explicitly set this up as Hadoop 2.5 plays nicely with zlib 1.2.7 and 1.2.8 versions.</p>

<ol>
  <li>Download Zlib compiled dll package from <a href="http://zlib.net/zlib128-dll.zip">here</a>, if that link doesn’t work, try <a href="http://prdownloads.sourceforge.net/libpng/zlib128-dll.zip?download">here</a>.</li>
  <li>Extract it to <code>C:\zlib-1.2.8</code></li>
  <li>Add <code>ZLIB_HOME</code> with value as <code>C:\zlib-1.2.8</code> to System Variables, and then append <code>%ZLIB_HOME%;</code> to <code>Path</code>.</li>
</ol>

<h5 id="optional-dependency-6-findbugs-139">Optional Dependency 6: Findbugs 1.3.9</h5>

<ol>
  <li>Download Findbugs 1.3.9 from <a href="http://switch.dl.sourceforge.net/project/findbugs/findbugs/1.3.9/findbugs-1.3.9.tar.gz">here</a>.</li>
  <li>Extract to <code>C:\findbugs-1.3.9</code>.</li>
  <li>Under Environment Variables, add a <code>FINDBUGS_HOME</code> system variable with value as  <code>C:\findbugs-1.3.9</code>.</li>
  <li>Append <code>%FINDBUGS_HOME%\bin;</code> to <code>Path</code>.</li>
</ol>

<h4 id="part-3-final-preparation-to-build">Part 3: Final Preparation To Build</h4>

<ol>
  <li>Add another System Variable under Environment Variables with the name <code>Platform</code> with value set to <code>x64</code>. This is case-sensitive. It’s <b>not</b> PLATFORM or platform, but <b><code>Platform</code></b>.</li>
  <li>
    <p>Build must be run through Windows SDK Command Prompt (Start &gt; All Programs &gt; 
Microsoft Windows SDK v7.1 &gt; Windows SDK 7.1 Command Prompt).</p>

    <p><img src="http://i.imgur.com/gZruIlT.png"></p>
  </li>
</ol>

<p><br />
Congratulations! You are now ready to build Hadoop. Move on to Chapter 3: Building Hadoop from Source.s</p>

<h4 id="to-be-updated">To Be Updated</h4>
]]></content>
  </entry>
  
</feed>
